# Secracy-System
I have created this simple C++ program that allows an user to encrypt or decrypt a message . It also uses a key which help the user to perform the task easily. I have also implemented a file handeling which helps to remembar  the encrypted or decrypted message and remembar the key.
